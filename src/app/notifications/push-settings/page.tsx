import { PushSettingsView } from "@/components/notifications/PushSettingsView";

export const metadata = {
  title: "Cài đặt thông báo trình duyệt — Gia phả dòng họ",
};

export default function PushSettingsPage() {
  return <PushSettingsView />;
}
