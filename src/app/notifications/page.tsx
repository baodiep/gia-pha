import { NotificationsView } from "@/components/notifications/NotificationsView";

export const metadata = {
  title: "Thông báo — Gia phả dòng họ",
};

export default function NotificationsPage() {
  return <NotificationsView />;
}
